**String functions**

1. ToString("X") converts it to hexadecimal meaning 0-9 + A-F
	x returns small case while X returns cap case
2. ToString("D4") represents Decimal with padding 0s for length 4
3. Instead of substring use indexing to take last 4 chars - [^4..]

**Shared**

1. Shared uses singleton instance for Random class in the program

**ClosedXML.Excel**

1. A library used to read and write Excel (.xlsx) files without needing Excel installed.