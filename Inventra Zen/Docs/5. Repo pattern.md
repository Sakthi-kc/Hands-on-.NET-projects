1. Create a Repo pattern class of type T which implements Interface of type T

2. This class is going to invoke context and dbset, dbset expects TEntity type hence declare where T: class

3. Create readonly variables for context and dbset which can be initialized in constructor
	Define them as protected because they may be used again in table specific repo

4. Dependency inject and get the ProductDBContext instance which is registered in program.cs

5. Assign context and dbSet with context.Set<T>() meaning whatever entity model it is

6. Implement the methods and make sure to set async/await as per the methods defined

7. All actions like add, update happens on table but save happens on DB as change tracking is global

8. Declare the service as scoped with (typeof(Interface<>), typeof(class<>)) in program.cs