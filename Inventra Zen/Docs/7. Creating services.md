1. Create an interface and class for performing services/ actions performed by the API

2. In the interface (no type required in declaration)

3. Pass the expected DTO model as datatype to the function definition

4. In the implementation class, we have to invoke the generic repo or table specific repo and map to entity model

5. Since the services are already added as scoped to program.cs, we can inject them in the service, which creates instance

6. Invoke the mapper also in this class, as we can perform all the business logic in service layer

7. Create readonly variables for mapper and repo which can be initialized in constructor
Define them as protected because they may be used again
