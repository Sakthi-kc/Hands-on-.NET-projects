1. Create an interface and class for table specific repository

2. In the interface (no type required in declaration), inherit the common interface with entity model type as it is specific to the table

3. In the table specific implementation class, implement the generic repo class and then table specific interface

4. The base class constructor will run first, hence pass the context to generic repo class

5. When any filter is based on computed column, add it explicitly in the function implementation

6. Now we can call the table specific repo from service class as it inherits base class methods

7. Declare the service as scoped in program.cs